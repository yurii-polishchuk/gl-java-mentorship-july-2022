# Spring
spring:
  main:
    bannerMode: console
  jpa:
    showSql: true
    properties:
      hibernate:
        jdbc:
        format_sql: true
  flyway:
    baselineOnMigrate: false
    enabled: false

# Logging
logging:
  config: classpath:log4j2-test.yml
