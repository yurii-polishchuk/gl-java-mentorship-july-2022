# Spring
spring:
  main:
    bannerMode: console
  # Data source
  dataSource:
    url: test
    username: test
    password: test
  jpa:
    showSql: true
  flyway:
    baselineOnMigrate: false
    enabled: false

# Logging
logging:
  config: classpath:log4j2-test.yml
